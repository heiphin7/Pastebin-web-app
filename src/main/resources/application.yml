spring:
  datasource:
      driver-class-name: org.postgresql.Driver
      url: jdbc:postgresql://localhost:5432/pastebin
      username: postgres
      password: admin
  jpa:
    hibernate:
      ddl-auto: update
server:
    port: 1448